<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="de" lang="de">
	<head>
		<meta http-equiv="content-type" content="text/html;charset=utf-8" />
		<meta name="language" content="de" />
		<meta name="author" content="xFastTemplate2" />
		<title>xFastTemplate2 - Error</title>
		<style type="text/css">
			.err_num, .err_msg, .err_job, .version, h1 {
				font-size: 12px;
				line-height: 1.4em;
				display: block;
				width: 600px;
				margin: auto auto;
				color: #990000;
				margin-bottom: 23px;
				font-family: Verdana, Arial, sans-serif;
			}
			h1 {
				font-size: 16px;
				font-weight: bold;
			}
			.version {
				font-variant: small-caps;
				letter-spacing: 0.1em;
				width: 600px;
				text-align: center;
				color: #999999;
			}
		</style>
	</head>
	<body>
		<!-- 
			You can build your own xFastTemplate2 error template by modify this file as you like it
			and place it inside your template directory within the same name.
			
			One personal stadement: an error message template doesn't need to be have valide code,
			like "oh - an img tag go an alt argument now!"
		-->
		<h1>{{ headline }}</h1>
		<p class="err_num">Number: {{ error_num }}</p>
		<p class="err_msg">Message: {{ error_msg }}</p>
		<p class="err_job">Method: {{ error_job }}</p>
		<p class="version">Version: {{ version }}</p>
	</body>
</html>